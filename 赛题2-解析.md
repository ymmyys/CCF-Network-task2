# 赛题2 解析：大模型推理算力资源动态负载感知调度

> 赛道：鹏城云脑 + 中国算力网（C²NET）/ OpenI 启智社区 · 国产异构算力（昇腾/海光/天数/燧原/壁仞/沐曦）
> 平台：https://openi.pcl.ac.cn/ · 不低于 100 张国产 AI 加速卡
> 初赛：2025/5/25–6/30 · 交付 = 设计方案 + 技术路线图 + ≤5min 原型演示视频

---

## 一、赛题原文要点

- 后端算力集群（GPU/NPU）可用性**非静态**，受三类因素制约：实例健康状态、瞬时负载波动、资源池隔离策略。
- 高并发 + 多资源池并存场景下，要消除"热点"，实现**高可用、高吞吐、低延迟**的请求调度。
- 静态轮询失效：负载分布不均 → 部分节点过载崩溃、其他节点空闲。
- **硬性要求**：基于华为昇腾 NPU（910/910B/910C 均可），设计实现一套**动态权重调度算法**，按节点实时负载动态调整被选中概率。
- **平滑过渡要求**：加权轮询场景下，某节点 capacity 大幅下降（如 10→1）时，需设计平滑过渡机制，**避免已分配请求被强制中断**。

---

## 二、问题的技术本质

赛题表面是"调度算法"，内核是一个**带动态权重的 LLM 推理负载均衡器（Router/LB）**。要同时解决三层问题：

| 层 | 问题 | 失败的旧做法 | 正确方向 |
|---|---|---|---|
| 感知 | 节点"现在还能吃多少请求"是动态的 | 静态权重/纯轮询 | 实时指标（队列、NPU 利用率、KV cache、延迟）折算容量 |
| 决策 | 按容量选节点、消除热点 | 静态加权轮询易爆发、易羊群 | 平滑加权轮询 + Power-of-Two |
| 过渡 | capacity 突变时不中断在途请求 | 直接摘节点/瞬间改权重 | 权重衰减 + drain + slow-start 回升 |

**为什么 LLM 场景特别难**：LLM 请求代价随输入/输出 token 数、KV cache 状态剧烈波动，单个请求的资源占用可差几十倍，传统按"连接数/QPS"的 LB 完全失准。这是我们方案立论的核心论据（见 Modular、vLLM Router 等参考）。

---

## 三、评分标准与投入映射（决定我们怎么拿第一）

| 评分项 | 权重 | 我们的对策 |
|---|---|---|
| **算力网特性利用程度** | **40%** | **最高优先级**。用昇腾 NPU 真实指标（npu-smi 利用率/显存、KV cache、队列、延迟）；体现多资源池隔离、910/910B/910C 异构差异化权重；接入 C²NET/启智真实多实例。 |
| 创意新颖性 | 30% | LA-SWRR：平滑加权轮询 ⊕ Power-of-Two ⊕ 负载折算有效权重 ⊕ 衰减/slow-start 状态机，比纯 SWRR/静态轮询有明确升级。 |
| 方案可行性 | 30% | 独立 Python Router 原型，真机可跑；静态轮询 vs LA-SWRR 对比实验出图；≤5min 演示视频。 |

**结论**：差异化得分点集中在 40% 的"算力网/昇腾特性"。算法要新颖（30%），但真正拉开差距的是**把昇腾真实指标和多资源池/异构这件事做实做透**。

---

## 四、关键约束与易踩的坑

1. **必须用昇腾 NPU**，不能拿 GPU 糊弄——指标采集要走 npu-smi / 昇腾生态（MindIE/CANN）。
2. **"平滑过渡"是显式评分锚点**：必须做出"capacity 10→1、在途请求零中断"的可视化对比，否则丢分。
3. **多资源池并存**是题面强调的场景——方案里要体现资源池隔离/跨池调度，不能只做单池。
4. 静态轮询要保留为**对照基线**，演示时才有对比说服力。
5. 初赛是"方案设计 + 原型验证"，不要求生产级规模——原型直观、对比清晰比堆功能更重要。

---

## 五、昇腾生态对接（40% 评分项的硬核支撑）

> 这一节是拿下"算力网特性利用程度 40%"的关键：证明我们用的是昇腾真实指标和真实生态，不是泛泛的 GPU 方案。

### 5.1 指标采集：直接复用 NPU Exporter → Prometheus（不用自己写采集）
昇腾官方 **NPU Exporter**（MindCluster 组件）周期轮询 NPU 驱动（`-updateTime` 默认 5s，可降到 1s），以 Prometheus 格式暴露每卡指标。可直接喂给我们的负载折算公式 `load_i`：

| Prometheus 指标 | 含义 | 用途（映射到 load_i） |
|---|---|---|
| `npu_chip_info_utilization` | AI Core 利用率 % | `npu_util` 主信号 |
| `npu_chip_info_hbm_used_memory` / `_hbm_total_memory` | HBM 显存 已用/总量 (MB) | KV cache/显存压力代理 `kv_cache_used` |
| `npu_chip_info_health_status` | 健康 1/0 | `health_i` 因子 |
| `npu_chip_info_error_code` | 错误码 | outlier 摘除触发 |
| `npu_chip_info_temperature` / `_power` | 温度/功耗 | 过热降权（异构差异化） |
| `npu_chip_info_aicore_current_freq` | AI Core 频率 | 算力档位 |

- 官方指标接口：[MindCluster Prometheus Metrics 接口](https://www.hiascend.com/document/detail/zh/mindcluster/71RC1/clustersched/dlug/dlug_api_004.html)
- 指标源码（指标名 ground truth）：[collector_for_npu.go](https://gitcode.com/Ascend/mind-cluster/blob/master/component/npu-exporter/collector/metrics/collector_for_npu.go)
- 华为云 CCE NPU 指标说明：[NPU 指标说明](https://support.huaweicloud.com/intl/zh-cn/usermanual-cce/cce_10_0970.html)
- 参考监控实践 + Grafana：[运维监控平台实践](https://www.hiascend.com/developer/techArticles/20251217-1)、[ascend-npu-dashboard](https://github.com/Cz200OK/ascend-npu-dashboard)

**推理引擎侧运行时指标**（队列长度、batch、KV block 占用、吞吐/延迟）从推理实例自身的 metrics 端点取：vLLM-Ascend 暴露 Prometheus 指标；MindIE Service 也有监控接口。两路指标合并成统一 `NodeState`。

### 5.2 MindIE 内置调度的关系：既复用、又要超越（新颖性论据）
MindIE Service（Motor 组件）已带 **Coordinator 调度器** 和 **PD 分离架构**（prefill/decode 分离，独立 Endpoint）：
- [Coordinator 调度器配置](https://www.hiascend.com/document/detail/zh/mindie/21RC2/mindieservice/servicedev/mindie_service0252.html)
- [PD 分离架构说明](https://www.hiascend.com/document/detail/zh/mindie/21RC1/mindieservice/servicedev/mindie_service0093.html)
- [MindIE Motor 简介](https://www.hiascend.com/document/detail/zh/mindie/300/mindiemotor/motordev/user_guide/introduction.md)

**定位策略**：我们的 LA-SWRR Router 挡在多个 MindIE/vLLM-Ascend Endpoint 之前（或对接 Coordinator 层），主打 MindIE 内置调度**没有强调的两点** —— ① 基于 NPU 真实指标的**动态负载感知权重**；② capacity 骤变（10→1）的**平滑过渡 + 零中断**。这就是相对官方调度器的新颖性增量，同时证明深度利用了昇腾生态。

---

## 六、调研结论（详见 PLAN 文档的参考清单）

- **算法骨架**：nginx 平滑加权轮询（SWRR），`effective_weight` 故障降/恢复缓升 → 天然平滑过渡机制。
- **防热点/防羊群**：Power-of-Two Choices（Ray Serve、SGLang 均有实现）。
- **LLM 专用路由**：vLLM Router（prefill/decode 感知）、KV-cache-aware routing、llm-d 分布式调度。
- **平滑/兜底范式**：Envoy outlier detection、K8s readiness/drain、slow-start。
- **论文（新颖性来源）**：SkyLB、Lodestar（在线学习路由）、Data-Parallel LB Bottleneck、Power-of-Two 综述。

> 我们的方案 = **LA-SWRR（Load-Aware Smooth WRR）**，详见同目录 `PLAN-赛题2.md`。
